<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= postArr.title %></h1>
    <p><%= postArr.description %></p>
    <img src="<%= postArr.imageUrl %>" alt="" />
    <p><%= postArr.imageUrl %></p>

    <form action="/admin/edit-post/<%= postArr._id %>" method="post">
      <button>Edit</button>
    </form>
    <form action="/admin/delete-post/<%= postArr._id %>" method="post">
      <button>Delete</button>
    </form>
  </body>
</html>
