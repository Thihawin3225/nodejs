<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= postArr.title %></h1>
    <p><%= postArr.description %></p>
    <img src="<%= postArr.image_url %>" alt="err" />
    <p><%= postArr.image_url %></p>
    <form action="/admin/post/<%= postArr.id %>" method="post">
      <button>Delete</button>
    </form>
    <a href="/admin/post-edit/<%= postArr.id %>">Edit</a>
  </body>
</html>
